# CUDA + LSTM Nifty 50 Forecasting

This project forecasts Nifty 50 prices using:
- Monte Carlo simulations (CPU & GPU)
- Multi-output LSTM model (LibTorch, CPU & GPU)
- Includes yearly percent error, return, accuracy, and validation loss tracking.
